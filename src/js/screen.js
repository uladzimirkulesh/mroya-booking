import * as Animations from './animations';

/*-------------------------------------------------------
Fire on window load
-------------------------------------------------------*/

window.addEventListener( 'load', () => {

	// Init Animations
	Animations.initAnimations();
} );
